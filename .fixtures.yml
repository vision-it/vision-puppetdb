---
fixtures:
  repositories:
    stdlib:
      ref: "v6.0.0"
      repo: "https://github.com/puppetlabs/puppetlabs-stdlib"
    docker:
      ref: "v3.7.0"
      repo: "https://github.com/puppetlabs/puppetlabs-docker"
    apt:
      ref: "v7.1.0"
      repo: "https://github.com/puppetlabs/puppetlabs-apt"
    vision_docker:
      repo: "https://github.com/vision-it/vision-docker"
      ref: "origin/production"
  symlinks:
    vision_puppetdb: '#{source_dir}'
